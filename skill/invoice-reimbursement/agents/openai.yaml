interface:
  display_name: "发票报销自动整理"
  short_description: "自动获取识别新增发票并规范命名后安全增量更新项目报销Excel工作簿"
  default_prompt: "Use $invoice-reimbursement to process newly added invoices and update the reimbursement workbook safely."
policy:
  allow_implicit_invocation: true
